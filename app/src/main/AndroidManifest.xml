<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.ifsul.pdm.frotacontrolfinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".SobreActivity"></activity>
        <activity android:name=".MainPassageiroActivity" />
        <activity
            android:name=".veiculo.VeiculoListActivity"
            android:label="@string/title_activity_veiculo_list"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".veiculo.VeiculoFormActivity" />
        <activity android:name=".MainSecretariaActivity" />
        <activity android:name=".CadastroSecretariaActivity" />
        <activity android:name=".LoginSecretariaActivity" />
        <activity android:name=".CadastroPassageiroActivity" />
        <activity android:name=".LoginPassageiroActivity" />
        <activity android:name=".SecretariaLC_Activity" />
        <activity android:name=".PassageiroLC_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

</manifest>